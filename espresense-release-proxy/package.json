{
  "name": "espresense-release-proxy",
  "private": true,
  "version": "1.0.0",
  "description": "ESPresense release proxy for Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest --verbose",
    "check": "wrangler deploy src/index.ts --dry-run",
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy src/index.ts",
    "format": "prettier --write '**/*.{js,ts,css,json,md}'"
  },
  "author": "Darrell Turner <DT@Terastar.biz>",
  "license": "MIT",
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251210.0",
    "@types/jest": "^28.1.6",
    "esbuild": "^0.15.5",
    "esbuild-jest": "^0.5.0",
    "jest": "^28.1.3",
    "jest-environment-wrangler": "^0.0.31",
    "prettier": "^1.18.2",
    "wrangler": "^4.54.0"
  }
}
